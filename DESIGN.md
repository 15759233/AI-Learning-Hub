# AI MAKER CAMPUS 总体视觉标准

## 1. 视觉定位

平台采用“**年轻创客校园 × 沉浸式 AI 实验室**”的双场景视觉体系。普通学习页面明亮、轻松、富有活力；模型部署、AI Agent、命令行等实训页面切换为深色沉浸模式。整体应显得年轻、专业、真实可用，避免传统高校官网、企业后台和夸张赛博朋克风格。

## 2. 色彩体系

- **品牌主色：** 活力橙 `#FF4D1F`，用于主按钮、当前导航、重点数字和行动入口。
- **页面背景：** 暖白或浅米灰 `#FBFAF7`，卡片使用纯白。
- **主文字：** 近黑色 `#111215`，辅助文字使用中性灰。
- **辅助色：** 紫色代表大模型，绿色代表 AI Agent，蓝色代表模型部署，黄色代表智能硬件，青绿色代表 AI 安全。
- 彩色只用于重点信息，页面主体仍以暖白、黑灰和留白为主，禁止大面积高饱和渐变。

## 3. 页面风格

页面采用清晰的 **Bento Grid 模块化布局**，通过不同尺寸的卡片形成节奏，但必须保持网格对齐。内容区留白充足，层级明确，不堆叠无意义装饰。首页和频道页可使用低饱和 3D 插画、半透明积木、网格与节点元素；课程、资源和实训卡片应优先展示真实内容缩略图。

## 4. 字体与排版

统一使用现代无衬线字体：`Inter`、`PingFang SC`、`Microsoft YaHei`。标题使用粗体和紧凑字距，形成明显视觉冲击；正文保持舒适行高和较高可读性。首页标题可大胆放大，内页标题克制统一。中文、英文术语和数字之间保留合理空格。

## 5. 卡片与组件

- 普通卡片使用 14–16px 圆角、浅色边框和轻微阴影。
- 大型推荐区或 Hero 区可使用 20–24px 圆角。
- 主按钮使用橙色实心样式，次按钮使用白底浅灰描边。
- 同一区域只保留一个最强主操作，避免橙色按钮过多。
- 同类卡片的封面比例、标题高度、信息顺序和按钮位置必须一致。

## 6. 深色实验室

实训工作台采用深蓝黑背景、分层面板和低饱和蓝紫描边。绿色仅表示运行成功，紫色表示流程节点或当前选择，橙色用于关键操作。终端、工作流、运行日志、任务指导和结果反馈必须分区清晰。深色模式只用于操作型页面，不扩展到全站。

## 7. 图片与图标

图标统一使用线性或双色 SVG，保持相同线宽与视觉尺寸。插画采用柔和 3D、等距视角和低饱和材质，避免儿童卡通、写实科幻和廉价霓虹。课程与实训封面统一使用固定比例，图片不得拉伸变形。

## 8. 动效原则

动效以轻量反馈为主：卡片悬停轻微上移，按钮颜色平滑变化，进度条和数字适度动画，实验节点运行时使用柔和呼吸效果。禁止频繁闪烁、夸张弹跳和大幅缩放。所有动效都应服务于状态表达和操作反馈。

## 9. 响应式与一致性

桌面端以 1440px 为设计基准，内容居中并限制最大宽度。页面缩小时，右侧栏下移、筛选栏折叠、卡片由多列变为双列或单列。全站必须统一 Logo、导航名称、分类颜色、卡片圆角、按钮样式、图标体系和页脚结构。

## 10. 视觉红线

- 不做传统高校门户网站。
- 不做 Element Plus 默认后台风格。
- 不使用大面积企业蓝或赛博朋克霓虹。
- 不混用多套字体、图标和插画风格。
- 不使用无法真实开发的异形布局。
- 不让装饰效果影响内容可读性和功能操作。
